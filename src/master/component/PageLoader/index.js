import React from "react";

const index = (props) => {
  return (
    <div
      style={{
        width: "100vw",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        background: "#44599c",
      }}
    >
      <h1 style={{ fontSize: "10vw", fontWeight: 900, color: "#fff" }}>
        Loading ...
      </h1>
    </div>
  );
};

export default index;
